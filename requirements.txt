# SuRaksha DocShield — Python dependencies
# Install: pip install -r requirements.txt

# Core
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Image processing
Pillow>=10.0.0
opencv-python-headless>=4.9.0
numpy>=1.26.0

# PDF support
PyPDF2>=3.0.0
pdf2image>=1.17.0        # requires poppler-utils on Linux

# OCR (install at least one)
pytesseract>=0.3.10      # requires tesseract-ocr system package
# easyocr>=1.7.0         # heavier but more accurate; runs fully offline after model download

# NLP / metadata
exifread>=3.0.0

# Dashboard
streamlit>=1.35.0
plotly>=5.20.0

# Optional: better NLP entity extraction
# spacy>=3.7.0           # run: python -m spacy download en_core_web_sm
