services:
  - type: web
    name: suraksha-docshield
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run frontend/dashboard.py --server.port $PORT --server.address 0.0.0.0 --server.headless true
    plan: free
